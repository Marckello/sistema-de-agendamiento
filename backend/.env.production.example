# ===========================================
# CitasPro - Variables de Entorno Producción
# ===========================================
# Copiar este archivo como .env y modificar los valores

# ===========================================
# DATABASE
# ===========================================
# En EasyPanel, usa el nombre del servicio de BD
DATABASE_URL=postgresql://postgres:TU_PASSWORD_SEGURO@citaspro-db:5432/agenda

# ===========================================
# JWT - CAMBIAR OBLIGATORIAMENTE
# ===========================================
# Generar con: openssl rand -base64 32
JWT_SECRET=CAMBIAR-POR-UN-SECRETO-SEGURO-DE-AL-MENOS-32-CARACTERES
JWT_EXPIRES_IN=7d

# ===========================================
# SERVER
# ===========================================
NODE_ENV=production
PORT=4000

# ===========================================
# EMAIL (Ejemplos para diferentes proveedores)
# ===========================================

# --- SendGrid ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxx

# --- Gmail ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=tu-app-password

# --- Amazon SES ---
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=tu-access-key
# SMTP_PASS=tu-secret-key

# --- Configuración actual ---
SMTP_HOST=smtp.tuservidor.com
SMTP_PORT=587
SMTP_USER=noreply@tudominio.com
SMTP_PASS=tu-password-smtp
EMAIL_FROM=CitasPro <noreply@tudominio.com>

# ===========================================
# FRONTEND URL (para links en emails)
# ===========================================
FRONTEND_URL=https://app.tudominio.com

# ===========================================
# CORS (dominios permitidos, separados por coma)
# ===========================================
CORS_ORIGINS=https://app.tudominio.com,https://tudominio.com
